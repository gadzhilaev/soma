name: soma
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  intl_utils: any
  flutter_svg: ^2.0.10+1

flutter:
  uses-material-design: true
  assets:
    - assets/logo/
    - assets/icons/
  
flutter_intl:
  enabled: true
